#pragma once

#include <string>

#include "IDAO.hpp"

class AccountDAO
{
    ID m_ID;               //[UNIQUE, NOT NULL]
    ID m_ProfileID = 0;    //[UNIQUE, NOT NULL]
    String m_Email = "";   //[UNIQUE, NOT NULL]

public:
    AccountDAO() = default;

//---------------------------------------------------------------------------------------

    AccountDAO(
        const String& email,
        const ID& profileID = 0,
        const ID& id = 0
    );

//---------------------------------------------------------------------------------------

    AccountDAO(const AccountDAO& other) = default;

//---------------------------------------------------------------------------------------

    ~AccountDAO() = default;

//---------------------------------------------------------------------------------------

    AccountDAO& operator=(const AccountDAO& other) = default;

//---------------------------------------------------------------------------------------

    String toString() const;

//---------------------------------------------------------------------------------------

    const ID& getID() const;

//---------------------------------------------------------------------------------------
    
    const ID& getProfileID() const;

//---------------------------------------------------------------------------------------

    const String& getEmail() const;

//---------------------------------------------------------------------------------------

    void setID(const ID& id);

//---------------------------------------------------------------------------------------

    void setProfileID(const ID& studentID);

//---------------------------------------------------------------------------------------

    void setEmail(const String& email);
};