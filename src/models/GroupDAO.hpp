#pragma once

#include "IDAO.hpp"

class GroupDAO
{
    ID m_ID;                //[UNIQUE, NOT NULL]
    String m_GroupName;    //[UNIQUE, NOT NULL]

public:

    GroupDAO() = default;

//---------------------------------------------------------------------------------------

    GroupDAO(const String groupName, const ID& id = 0);

//---------------------------------------------------------------------------------------

    GroupDAO(const GroupDAO& other) = default;

//---------------------------------------------------------------------------------------

    ~GroupDAO() = default;

//---------------------------------------------------------------------------------------

    GroupDAO& operator=(const GroupDAO& other) = default;

//---------------------------------------------------------------------------------------

    String toString();

//---------------------------------------------------------------------------------------

    const ID& getID() const;

//---------------------------------------------------------------------------------------

    const String& getGroupName() const;

//---------------------------------------------------------------------------------------

    void setID(const ID& id);

//---------------------------------------------------------------------------------------

    void setGroupName(const String& groupName);
};